# Web application dependencies for SFU voice chat

# Core web framework
fastapi>=0.104.0        # Modern, fast web framework with async support
uvicorn>=0.24.0         # ASGI server to run FastAPI
websockets>=12.0        # WebSocket support for real-time communication

# Additional web utilities
python-multipart>=0.0.6 # For form data parsing (if needed)
jinja2>=3.1.2           # HTML templating (for serving web pages)

# Existing dependencies (from requirements.txt)
numpy>=1.24.0           # Audio processing and mixing
scipy>=1.10.0           # Signal processing (filters)
pycryptodome>=3.19.0    # Encryption (AES, RSA, HMAC)
reedsolo>=1.7.0         # Error correction (may not need for real-time)
